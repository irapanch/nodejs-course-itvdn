const calculateArea = require("./003_rectangle");
const length = 6;
const width = 4;
const area = calculateArea(length, width);
console.log(module);
console.log(`The area of the rectangle is ${area}`);
